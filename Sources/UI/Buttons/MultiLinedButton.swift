//
//  File.swift
//  
//
//  Created by David Casserly on 30/06/2021.
//

import Foundation

/*
 
 public func setTitleAndSubtitle(title: String, subtitle: String?) {
     if let subtitle = subtitle {
         buttonHeightConstraint?.constant = 55
         cornerRadius = 55 / 2.0
         
         titleLabel?.lineBreakMode = .byWordWrapping
         titleLabel?.numberOfLines = 2
         titleLabel?.textAlignment = .center
                 
         let buttonString = NSMutableAttributedString()
         
         let titleFont = [NSAttributedString.Key.font: FontStyle.buttonText.font]
         let titleString = NSAttributedString(string: title, attributes: titleFont)
         buttonString.append(titleString)
         buttonString.append(NSAttributedString(string: "\n"))
         
         let subtitleFont = [NSAttributedString.Key.font: FontStyle.buttonTextSmall.font]
         let subtitleString = NSAttributedString(string: subtitle, attributes: subtitleFont)
         buttonString.append(subtitleString)
         
         setAttributedTitle(buttonString, for: .normal)
     } else {
         setTitle(title, for: .normal)
     }
 }
 
 */
